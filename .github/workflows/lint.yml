name: Lint

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  lint-chart:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: azure/setup-helm@v4
      - run: helm lint . -f values-example.yaml --set image.tag=test
